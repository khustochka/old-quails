%table.bordered
  %tr
    %th.numeric Sort
    %th Name la
    %th Name en
    %th Name ru
    %th Name uk
    %th Syn
    %th Descr
    %th
      %em del
  - hierarchy_cells(@bunch, @proceed_methods.dup) do |item|
    %tr
      %td.numeric
        = item.sort
      %td
        = link_to item.name_la, item
      %td
        = item.name_en
      %td
        = item.name_ru
      %td
        = item.name_uk
      %td.boolean
        = !item.synonims.empty? ? "+" : "-"
      %td.boolean
        = !item.description.empty? ? "+" : "-"
      %td
        [
        = link_to 'x', item, :confirm => 'Are you sure?', :method => :delete
        ]
